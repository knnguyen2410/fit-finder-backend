package com.example.fitfinder;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main class that serves as the entry point for the FitFinder application.
 */
@SpringBootApplication
public class FitFinderApplication {

	/**
	 * The main method that starts the FitFinder application.
	 *
	 * @param args The command-line arguments passed to the application.
	 */
	public static void main(String[] args) {
		SpringApplication.run(FitFinderApplication.class, args);
	}

}
